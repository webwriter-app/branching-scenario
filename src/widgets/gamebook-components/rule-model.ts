interface Rule {
  output_id: string;
  element: string;
  condition: string;
  match: string;
  target: string;
  isConditionEnabled: Boolean;
  isMatchEnabled: Boolean;
  isTargetEnabled: Boolean;
}
